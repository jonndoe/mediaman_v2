import { AbstractMedia.Service } from './abstract-media.service';

describe('AbstractMedia.Service', () => {
  it('should create an instance', () => {
    expect(new AbstractMedia.Service()).toBeTruthy();
  });
});
